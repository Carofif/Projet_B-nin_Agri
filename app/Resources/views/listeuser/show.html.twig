{% extends 'base.html.twig' %}

{% block content %}
    <h1>Listeuser</h1>



    <ul>
        <li>
            <a href="{{ path('listeuser_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% block content %}

    <div class="card">
        <div class="card-body">
            <h4 class="card-title"></h4>
            <h1>Offre</h1>
            <div class="row">
                <div class="col-sm-12 col-md-6">
                    <img src="{{  asset('uploads/offreImages/' ~ offre.image) }}" class="img-fluid" alt="image"/>
                </div>
                <div class="col-sm-12 col-md-6">
                    <table>
                        <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ listeUser.id }}</td>
                        </tr>
                        <tr>
                            <th>Nom</th>
                            <td>{{ listeUser.nom }}</td>
                        </tr>
                        <tr>
                            <th>Prenom</th>
                            <td>{{ listeUser.prenom }}</td>
                        </tr>
                        <tr>
                            <th>Telephone</th>
                            <td>{{ listeUser.telephone }}</td>
                        </tr>
                        </tbody>
                    </table> </table>

                </div>
            </div>
            {<ul>
                <li>
                    <a href="{{ path('demande_new') }}" class="btn btn-success">Acceder à l'offre </a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
