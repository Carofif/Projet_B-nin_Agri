{% extends 'base.html.twig' %}

{% block content %}

  <div class="row">
  
		<div class="col-md-12">
			 <a class="btn btn-light" href="{{ path('produit_index') }}"><span class=" mdi mdi-arrow-left">Retour a la liste<span/></a>
		</div>
	</div><br>
  <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Champ N°{{ champ.id }}</h4>
    
    <table class='table'>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ champ.id }}</td>
            </tr>
            <tr>
                <th>Superficie</th>
                <td>{{ champ.superficie }}</td>
            </tr>

        </tbody>
    </table>

	
<br>
   <div class="row">
		<div class="col-md-2" style="margin-right = 10"> 
		
		<a  class="btn btn-info" href="{{ path('champ_edit', { 'id': champ.id }) }}"><span class="mdi mdi-stackexchange">Modifier<span/></a>
		</div>
		
		<div class="col-md-1">
		</div>
		<div class="col-md-2">
		 {{ form_start(delete_form) }}
				<button type="submit" class="btn btn-danger"><span class=" mdi mdi-delete "></span>Supprimer</button>
		 
            {{ form_end(delete_form) }}
			</div>
   </div><br>
 
	</div>
					</div>
					</div>
					</div>
{% endblock %}
