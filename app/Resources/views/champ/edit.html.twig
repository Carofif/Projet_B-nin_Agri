{% extends 'base.html.twig' %}

{% block content %}

   <div class="row">
		<div class="col-md-12">
			 <a class="btn btn-light" href="{{ path('champ_index') }}"><span class=" mdi mdi-arrow-left">Retour a la liste<span/></a>
		</div>
   </div><br>
  <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Modification d'un champ</h4>
					
  {{ form_start(edit_form) }}
	
		<div class="row">
			<div class="col-md-6">
				<label for="">Superficie</label>
				 {{ form_widget(edit_form.superficie , {'attr' : {'class' : 'form-control'}}) }}
					<span class="text-danger">{{ form_widget(edit_form.superficie) }}</span>
			</div>
			{#<div class="col-md-6">#}
				{#<label for="">Zone</label>#}
				 {#{{ form_widget(edit_form.zone , {'attr' : {'class' : 'form-control'}}) }}#}
					{#<span class="text-danger">{{ form_errors(edit_form.zone) }}</span>#}
			{#</div>#}
		</div>
		<br>
		<div class="row">
			<div class="col-lg-12">
				<div align="right">
					<input class="btn btn-success" type="submit" value="Modifier" />
				</div>
			</div>
		</div>


    <ul>
	
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
	</div>
					</div>
					</div>
					</div>
{% endblock %}
